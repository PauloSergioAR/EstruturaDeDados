struct node {
	int info;
	struct node* next;
};

typedef struct node Node;

Node* init(){
	return NULL;

}

Node* insere(Node* n){
	Node* novo = (Node*)malloc(sizeof(Node));
	Node* q;
	
	while(q->next != NULL){
		q = q->next;		
	}
}
